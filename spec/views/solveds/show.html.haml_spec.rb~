require 'spec_helper'

describe "solveds/show" do
  before(:each) do
    @solved = assign(:solved, stub_model(Solved))
  end

  it "renders attributes in <p>" do
    render
    # Run the generator again with the --webrat flag if you want to use webrat matchers
  end
end
